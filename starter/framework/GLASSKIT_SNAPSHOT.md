# GlassKit framework snapshot

This directory is the self-contained GlassKit runtime shipped with the starter template. It mirrors the reusable framework modules in `src/` at the time the starter is updated.

New app-specific behaviour belongs in `../app.js`. Framework fixes should be made in the source framework and then synced into this snapshot.
